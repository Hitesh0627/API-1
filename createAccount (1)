const axios = require('axios');

async function createAccount(apiend, data) {
  try {
    const response = await axios.post(apiend, data);

    if (response.status === 200) {
      console.log('account created successfully');
      console.log(response.data);
    } else {
      console.log('account not created');
      console.log(response.status);
    }
  } catch (error) {
    console.error(error.message);
  }
}

const details = {
  name: 'Hitesh',
  email: 'hitesh0627.be21@chitkara.edu.in',
  rollNumber: 2110990627,
  phone: 9350142139,
};

// Replace 'your-api-endpoint' with the correct API endpoint provided by Salesforce
const apiend = 'https://your-api-endpoint.com/services/apexrest/createAccount';

createAccount(apiend, details)